/*

function authInfo(response) {
  if (response.session) {
//    alert('user: '+response.session.mid);
  } else {
//    alert('not auth');
  }
}
// VK.Auth.getLoginStatus(authInfo);
*/
// VK.UI.button('vk_login');

//VK.Widgets.Auth("vk_login", {width: "150px", authUrl: '/vk/login?'});
